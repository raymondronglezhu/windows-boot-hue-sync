[project]
name = "hue-agent"
version = "0.1.0"
description = "Minimal Philips Hue setup helper for Windows Boot Hue Sync"
requires-python = ">=3.11"
readme = "README.md"

[project.scripts]
hue-agent = "hue_agent.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
